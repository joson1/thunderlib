menu "GUI"
depends on FB_EN


config GUI_LVGL
    bool "use lvgl"
    help
        use lvgl as gui

if GUI_LVGL
source "packages/gui/lvgl/Kconfig"
endif

config GUI_GUILITE
    bool "use guilite"
    help
        use guilite as gui


endmenu
